name: CI

on:
  pull_request:
  push:
    branches: [main]

env:
  VITE_FIREBASE_API_KEY: ${{ vars.VITE_FIREBASE_API_KEY }}
  VITE_FIREBASE_AUTH_DOMAIN: ${{ vars.VITE_FIREBASE_AUTH_DOMAIN }}
  VITE_FIREBASE_PROJECT_ID: ${{ vars.VITE_FIREBASE_PROJECT_ID }}
  VITE_FIREBASE_STORAGE_BUCKET: ${{ vars.VITE_FIREBASE_STORAGE_BUCKET }}
  VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ vars.VITE_FIREBASE_MESSAGING_SENDER_ID }}
  VITE_FIREBASE_APP_ID: ${{ vars.VITE_FIREBASE_APP_ID }}
  VITE_ENABLE_APP_CHECK: ${{ vars.VITE_ENABLE_APP_CHECK }}
  VITE_FIREBASE_APP_CHECK_RECAPTCHA_SITE_KEY: ${{ vars.VITE_FIREBASE_APP_CHECK_RECAPTCHA_SITE_KEY }}
  VITE_ENABLE_APP_CHECK_IN_DEV: ${{ vars.VITE_ENABLE_APP_CHECK_IN_DEV }}
  VITE_ENABLE_OFFLINE_PERSISTENCE: ${{ vars.VITE_ENABLE_OFFLINE_PERSISTENCE }}
  VITE_ENABLE_FCM: ${{ vars.VITE_ENABLE_FCM }}
  VITE_FIREBASE_FCM_VAPID_KEY: ${{ vars.VITE_FIREBASE_FCM_VAPID_KEY }}
  VITE_LOG_LEVEL: ${{ vars.VITE_LOG_LEVEL }}

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run test
      - run: npm run build

  functions:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: functions
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: functions/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - run: npm i -g firebase-tools
      - run: firebase emulators:exec --only firestore "npm test -- --runInBand"

  rules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - run: npm ci
      - run: npm i -g firebase-tools
      - run: firebase emulators:exec --only firestore "npx jest firestore/rules.test.cjs"
