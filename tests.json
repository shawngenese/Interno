{
  "name": "interno-tests",
  "scripts": {
    "test": "npm run test:unit && npm run test:rules",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:rules": "firebase emulators:exec --only firestore 'node firestore/rules.test.js'",
    "test:build": "tsc -b && vite build",
    "test:edge": "echo 'Run edge function tests via Supabase CLI: supabase functions test'"
  }
}
